# Loading
- __The way the stylesheet (eg. colours) and the main script (eg. layout) loads has been changed__
    - The stylesheet will now attempt to load as soon as the browser allows. This seems to greatly prevent flashes of unthemed white pages when Last.fm decides to reload the page. The layout will seem broken while it's loading but the page will match your theme (like if you chose dark). **If you were using Stylus to load the theme along with Tampermonkey, please try this out long term as Stylus is not recommended.**

# 2025.0113.1
- Fixed crashes related to Pro accounts without custom badges